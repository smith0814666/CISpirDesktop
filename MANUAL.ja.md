# CISpir マニュアル

**言語 / Languages:** [English](MANUAL.md) · [中文](MANUAL.zh.md) · [日本語](MANUAL.ja.md)

CISpir は音声・動画・テキストを理解可能なインプットの語学学習教材に変えるデスクトップアプリで、テキスト読み上げ・音声認識・字幕編集・Anki 書き出し・練習ツールを備えています。日本語と英語の学習向けです。

## 目次

1. [はじめに](#1-はじめに)
2. [テキスト読み上げ（TTS）](#2-テキスト読み上げtts)
3. [音声認識（書き起こし）](#3-音声認識書き起こし)
4. [字幕ワークベンチと Anki](#4-字幕ワークベンチと-anki)
5. [練習・学習ツール](#5-練習学習ツール)
6. [リラックス・集中](#6-リラックス集中)
7. [履歴・バックアップ・設定](#7-履歴バックアップ設定)
8. [アップデート](#8-アップデート)
9. [トラブルシューティング](#9-トラブルシューティング)

## 1. はじめに

- 初回起動時に**学習する言語**（日本語または英語）を選択します。これは音声コンテンツの言語、メディア認識言語、表示される日本語専用ツールを決めます。
- **インターフェース言語**（簡体字中国語 / English / 日本語）は学習言語とは独立しています。中国語の画面で日本語を学ぶこともできます。
- **Groq API キー**は AI 書き起こし（音声 → テキスト）にのみ必要です。Groq で `gsk_…` キーを取得し、設定に貼り付けます。システムのキーチェーンに暗号化して保存され、履歴やプロジェクトファイルには保存されません。

## 2. テキスト読み上げ（TTS）

1. 日本語または英語のテキストを入力します。
2. 音声スタイル（自然・落ち着いた・やさしい・元気・スロー学習など）を選びます。
3. **生成**をクリック。長文は自動で文ごとに分割され、文単位のタイムスタンプ付きで 1 つの MP3 に結合されます。

- 任意の文をクリックして再生できます。単文ループ・速度調整・シャドーイングに対応。
- **対話モード**：複数のキャラクターのセリフを順に追加し、それぞれに名前と音声を設定して 1 つの MP3 に合成します。
- **ふりがな / 読み方修正**（日本語のみ）：固有名詞や難読語に正しい読みを指定でき、ルールは全文に適用されます。
- MP3 とタイムスタンプファイルを書き出せます。

## 3. 音声認識（書き起こし）

1. 「メディアを読み込む」から、YouTube リンク、Bilibili リンク（パート指定可）、ローカルの音声/動画ファイル、またはポッドキャストを選びます。
2. 元に公式字幕がある場合は、CISpir がそれを優先して使います（API コストなし）。
3. ない場合は、あなたの確認後に必要に応じてメディアをダウンロードし、Groq Whisper で書き起こします。

- 対応するローカル音声：MP3、WAV、M4A、FLAC、OGG、WebM。動画：MP4、WebM、MKV、MOV。1 ファイル最大 2 GB、1 メディア最長 4 時間。
- 中断された書き起こしは下書きとして保存され、「処理を続行」で最初からやり直せます。
- 書き起こし結果（タイトル・文ごとのテキスト・開始/終了時刻）を編集でき、再書き起こしはプレビュー確認後に現在の字幕と置き換えます。

## 4. 字幕ワークベンチと Anki

- 字幕ワークベンチでは各文のテキストとタイミングを編集できます。
- 文・単語・フレーズを Anki に保存できます。**AnkiConnect** アドオン（既定ポート `8765`）をインストールして有効にし、Anki を起動したままにしてください。カードは `TextVoice` デッキに保存されます。
- 各カードにはその文全体の音声（文の前 0.5 秒、後 1 秒）が含まれます。

## 5. 練習・学習ツール

- **音声練習**：マイクに向かって話すと書き起こされます。
- **日本語専用ツール**（日本語学習のみ）：動詞/形容詞の活用練習、クイズ、ふりがな付与。
- **AI 会話練習**：AI と対話形式で練習します。
- **NHK Easy News**（日本語のみ）：記事リンクを貼るとふりがな付きで本文を読み込みます。

## 6. リラックス・集中

- 環境音シーン、ホワイト / ピンク / ブラウンノイズ、バイノーラルビート、ポモドーロタイマー。

## 7. 履歴・バックアップ・設定

- 履歴は最大 100 件のセッション（保存したテキスト・音声・書き起こし）を保持します。セッションを削除するとその音声も削除されます。
- 全量バックアップの書き出し / 読み込みに対応。

## 8. アップデート

- 「その他 → 更新を確認」が公開 GitHub Releases から新バージョンを確認します。Windows インストーラー版は確認後に自動インストール、macOS と Windows ポータブル版はダウンロードリンクを開きます。

## 9. トラブルシューティング

- **書き起こしが失敗する**：同梱の ffmpeg / yt-dlp が見つからない可能性があります。アプリを再インストールすると復元されます。システム PATH にはフォールバックしません。
- **Groq キー**：AI 書き起こしには有効な `gsk_…` キーが必要です。
- **Anki 保存に失敗する**：AnkiConnect がインストールされ、Anki が起動していることを確認してください。
- **macOS「身元不明の開発元」**：ダウンロードガイド（README）を参照してください。
- **TTS はネット接続が必要**：音声は Microsoft Edge TTS でオンライン生成されます。
