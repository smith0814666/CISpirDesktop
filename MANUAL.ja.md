# CISpir マニュアル

**言語 / Languages:** [English](MANUAL.md) · [中文](MANUAL.zh.md) · [日本語](MANUAL.ja.md)

CISpir は音声・動画・テキストを理解可能なインプットの語学学習教材に変えるデスクトップアプリで、テキスト読み上げ・音声認識・字幕編集・Anki 書き出し・練習ツールを備えています。日本語と英語の学習向けです。

## 目次

1. [はじめに](#1-はじめに)
2. [テキスト読み上げ（TTS）](#2-テキスト読み上げtts)
3. [音声認識（書き起こし）](#3-音声認識書き起こし)
4. [字幕ワークベンチと Anki](#4-字幕ワークベンチと-anki)
5. [練習・学習ツール](#5-練習学習ツール)
6. [リラックス・集中](#6-リラックス集中)
7. [設定](#7-設定)
8. [履歴とバックアップ](#8-履歴とバックアップ)
9. [アップデート](#9-アップデート)
10. [トラブルシューティング](#10-トラブルシューティング)

## 1. はじめに

- 初回起動時に**学習する言語**（日本語または英語）を選択します。これは音声コンテンツの言語、メディア認識言語、表示される日本語専用ツールを決めます。
- **インターフェース言語**（簡体字中国語 / English / 日本語）は学習言語とは独立しています。中国語の画面で日本語を学ぶこともできます。
- **Groq API キー**は AI 書き起こし（音声 → テキスト）にのみ必要です。Groq で `gsk_…` キーを取得し、設定に貼り付けます。システムのキーチェーンに暗号化して保存され、履歴やプロジェクトファイルには保存されません。

## 2. テキスト読み上げ（TTS）

**用途**：任意の日本語・英語のテキストを自然な音声に変換し、聞く・シャドーイングする・文ごとに学ぶためのものです。アプリの「テキスト → 理解可能なインプット」の中核です。

**操作手順：**

1. テキストを入力します。2 つのモードがあります：
   - **文章**——1 つのまとまったテキスト。
   - **対話**——複数のキャラクターのセリフで、それぞれに名前と音声を設定し、1 つの MP3 に合成します。
2. （日本語のみ）必要に応じて**読み方修正**：固有名詞や難読語に正しいふりがなを指定します。ルールは全文に適用され、この端末に保存されます。
3. **音声スタイル**（自然・落ち着いた・やさしい・元気・スロー学習など）を選びます。
4. **生成**をクリック。長文は自動で文ごとに分割され、文単位のタイムスタンプ付きで 1 つの MP3 に結合されます。
5. **結果を学習する：**
   - 任意の文をクリックして再生します。
   - 単文ループ、速度調整、**シャドーイング**（音声に続いて復唱）に対応。
   - 各文にタイムスタンプがあるため、どこへでもジャンプできます。
6. MP3 とタイムスタンプファイルを**書き出し**ます。

## 3. 音声認識（書き起こし）

**用途**：学習したい音声・動画を文単位のテキストと字幕に変換します。「音声・動画 → 理解可能なインプット」のもう半分です。

**操作手順：**

1. 「メディアを読み込む」を開きます。
2. ソースを選びます：
   - **YouTube**——リンクを貼り付け。
   - **Bilibili**——リンクを貼り付け（パート指定可）。
   - **ローカルファイル**——音声（MP3、WAV、M4A、FLAC、OGG、WebM）または動画（MP4、WebM、MKV、MOV）、最大 2 GB・4 時間。
   - **ポッドキャスト**——番組名で検索、または公開 RSS/Atom・Apple Podcasts リンクを貼り付け。
3. 元に**公式字幕**があれば、CISpir が自動で使います（費用も API キーも不要）。
4. 字幕がない場合、CISpir が確認を求め、必要に応じて音声をダウンロードし、**Groq Whisper** で書き起こします（Groq キーが必要。設定を参照）。
5. **結果を学習する**：文単位の字幕。任意の行をクリックして再生・ループできます。
6. **編集**：タイトル、各文のテキスト、開始/終了時刻を変更できます。再書き起こしはプレビューを生成し、確認後に現在の字幕と置き換えます。

- 中断された書き起こしは**下書き**として保存され、「処理を続行」で最初からやり直せます。

## 4. 字幕ワークベンチと Anki

**用途**：書き起こし結果を学習教材に変えます——編集、AI による文ごとの分析、文・単語の音声付き Anki カードへの書き出し（間隔反復用）。

**操作手順：**

1. メディアのセッションから**字幕ワークベンチ**を開きます。
2. 任意の文をクリックして再生・ループ、またはテキストとタイミングを編集します。
3. **AI 分析**（設定で AI の構成が必要）：文を選択して「分析」を押すと、翻訳・文法解析・語彙抽出が得られます。全文の**概要**（要約・トピック・難易度・重要語彙）も生成できます。
4. **Anki に保存**：文・単語・フレーズを保存します。**AnkiConnect** アドオン（既定ポート `8765`）をインストールして有効にし、Anki を起動したままにしてください。カードは `CISpir` デッキに保存されます（設定 → Anki で変更可）。
5. 各カードにはその文全体の音声（文の前 0.5 秒、後 1 秒）が含まれます。

## 5. 練習・学習ツール

**用途**：スピーキング・文法・会話を能動的に練習し、取り込んだ内容を定着させます。

- **音声練習**：マイクに向かって話すと書き起こされます（初回はマイク権限の許可が必要）。
- **日本語の活用練習とクイズ**（日本語学習のみ）：動詞/形容詞の活用を練習し、クイズに答えます。
- **AI 会話練習**：AI と対話して言語を練習します。
- **NHK Easy News**（日本語のみ）：記事リンクを貼ると、ふりがな付きで本文を読み込み、読解練習ができます。

## 6. リラックス・集中

**用途**：休める静かな空間、または学習時間を計る集中タイマーです。

- **環境音シーン**、ホワイト / ピンク / ブラウンノイズ、**バイノーラルビート**で背景の雰囲気を作ります。
- **ポモドーロタイマー**は学習ワークスペースに入ると自動で開始し、集中の終了ごとに休息を促します（時間は「設定 → ポモドーロ」で変更）。

## 7. 設定

上部バーから「設定」を開きます。設定はタブごとに分かれています。

### 外観

- **テーマ**：システムに従う / ライト / ダーク。
- **文字サイズ**：読みやすい / 大きい。

### AI（字幕 AI）

学習ワークスペースで各文の翻訳・文法解析・語彙抽出、全文概要の生成に使う AI を設定します。

- **AI プロバイダー**：Groq、またはカスタム（OpenAI 互換）。
- **API アドレス**：ベース URL（カスタムプロバイダーのみ必要）。
- **API キー**：あなたのキー。OS で暗号化して保存され、履歴やプロジェクトファイルには書き込まれません。
- **AI モデル**：モデル名。
- **解説言語**：AI の解説に使う言語。表示言語に合わせることもできます。
- **回答スタイル**：正確 / バランス。
- **タイムアウト**：リクエストのタイムアウト。
- **接続テスト**で確認し、**保存**で保存、**キーを削除**で保存済みキーを削除します。

### Groq（書き起こし）

- **Groq API キー**：AI 書き起こし（音声 → テキスト）に必要です。`gsk_…` キーを入力します。システムのキーチェーンに暗号化して保存されます。

### ポモドーロ

- **学習ワークスペースで自動的に計時**：有効にするとワークスペースに入ると自動で開始し、離れると一時停止します。
- **集中 / 短い休憩 / 長い休憩**：それぞれの分数。範囲——集中 5–90、短い休憩 1–30、長い休憩 5–60。
- **集中 4 回**ごとに長い休憩に入ります。新しい時間は次のサイクルから反映されます。

### Anki

- **Anki デッキ名**：カードを保存するデッキ（既定 `CISpir`）。

### その他の設定

- **インターフェース言語**：簡体字中国語 / English / 日本語。いつでも切り替え可能で、学習言語とは独立しています。
- **学習する言語**：日本語または英語（初回起動時に設定、上部バーから変更可）。TTS の内容言語と表示される学習ツールを決めます。
- **読み方修正**（日本語のみ）：固有名詞や難読語に正しいふりがなを指定でき、ルールはこの端末に保存され全文に適用されます。

## 8. 履歴とバックアップ

- 履歴は保存したセッション（テキスト・音声・書き起こし）を件数制限なしで保持します。セッションを削除するとその音声も削除されます。
- 全量バックアップの書き出し / 読み込みに対応（全レコード書き出し / バックアップから読み込み）。

## 9. アップデート

- 「その他 → 更新を確認」が公開 GitHub Releases から新バージョンを確認します。Windows インストーラー版は確認後に自動インストール、macOS と Windows ポータブル版はダウンロードリンクを開きます。

## 10. トラブルシューティング

- **書き起こしが失敗する**：同梱の ffmpeg / yt-dlp が見つからない可能性があります。アプリを再インストールすると復元されます。システム PATH にはフォールバックしません。
- **Groq キー**：AI 書き起こしには有効な `gsk_…` キーが必要です。
- **Anki 保存に失敗する**：AnkiConnect がインストールされ、Anki が起動していることを確認してください。
- **macOS「身元不明の開発元」**：ダウンロードガイド（README）を参照してください。
- **TTS はネット接続が必要**：音声は Microsoft Edge TTS でオンライン生成されます。
